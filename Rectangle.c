#include <stdio.h>

int main()
{
    int length, breadth, perimeter;
    length = 7;
    breadth = 5;
    perimeter = 2 * (length + breadth);
    printf("perimerer of rectangle:%d", perimeter);
    return 0;
}
